import { SilverDirective } from './silver.directive';

describe('SilverDirective', () => {
  it('should create an instance', () => {
    const directive = new SilverDirective();
    expect(directive).toBeTruthy();
  });
});
